//Map vs forEach

//MAP
const array = [1, 2, 3, 4, 5];
array.map((item) => item * 2); //resultado => [2, 4, 6, 8, 10]

//forEach
const array = [1, 2, 3, 4, 5];
array.forEach((item) => item * 2);//resultado => undefined